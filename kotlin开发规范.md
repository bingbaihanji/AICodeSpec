# Kotlin 开发规范

  >  kotlin 开发规范

  ## 1. 源文件与目录结构

  - **【强制】** 目录结构遵循包名，省略公共根包。如 `org.example.project` 对应目录 `org/example/project`。
  - **【强制】** 源文件以 `.kt` 结尾，若只含一个顶层类，文件名与类名相同（驼峰风格，首字母大写）；含多个类或顶层声明时，取描述性名称，避免 `Util` 等无意义词。
  - **【推荐】** 多平台项目中，平台特有文件加平台后缀（如 `Platform.jvm.kt`），公共文件不加后缀。
  - **【推荐】** 一个文件可包含语义紧密关联的多个声明，保持文件大小合理（不超过几百行）。

  ## 2. 类与声明布局

  - **【推荐】** 类内容顺序：属性与初始化块 → 次构造函数 → 方法 → 伴生对象。
  - **【推荐】** 方法不按字母或可见性排序，相关方法放在一起；重载方法靠拢放置。
  - **【推荐】** 实现接口时，成员顺序与接口声明顺序一致。
  - **【推荐】** 嵌套类紧挨使用它们的代码，外部使用的嵌套类放在末尾。

  ## 3. 命名规则

  | 类型                  | 风格                           | 示例                            |
  | --------------------- | ------------------------------ | ------------------------------- |
  | 包名                  | 全小写，无下划线，多个词可连接 | `org.example.myproject`         |
  | 类/对象               | 大驼峰（PascalCase）           | `UserService`, `MainActivity`   |
  | 函数/属性/变量        | 小驼峰（camelCase）            | `processData`, `orderCount`     |
  | 常量（const/顶层val） | 全大写，下划线分隔             | `MAX_COUNT`, `USER_NAME_FIELD`  |
  | 枚举常量              | 全大写或大驼峰，按用途选择     | `RED`, `GREEN` 或 `Pending`     |
  | 幕后属性              | 私有属性加 `_` 前缀            | `_elementList`                  |
  | 测试方法              | 反引号含空格或下划线分隔       | `` `ensure everything works` `` |

  - **【强制】** 避免无意义词汇（`Manager`、`Wrapper`）。
  - **【强制】** 缩写规则：两个字母全大写（`IOStream`），更长则首字母大写（`XmlFormatter`）。

  ## 4. 格式规范

  ### 4.1 缩进与括号
  - **【强制】** 使用 4 空格缩进，禁用 tab。
  - **【强制】** 左花括号放在行尾，右花括号独占一行，`else`/`catch`/`finally` 与右花括号同行。
  - **【强制】** 对于单条语句的 `if`/`for`/`while` 也需使用花括号（不省略）。

  ### 4.2 空格
  - **【强制】** 二元运算符两侧加空格（`a + b`），`..` 运算符两侧不加空格。
  - **【强制】** 一元运算符不加空格。
  - **【强制】** 控制流关键字（`if`, `when`, `for`, `while`）后左括号前留空格。
  - **【强制】** 方法声明/调用的左括号前不加空格。
  - **【强制】** 括号内部不加空格，`.` `?.` 前后不加空格，`//` 后留空格，`::` 前后不加空格，可空类型 `?` 前无空格。
  - **【强制】** 横向对齐避免，重命名不应影响格式。

  ### 4.3 冒号
  - 分隔类型与超类型或 `object` 关键字后时，冒号前留空格；声明类型时冒号前无空格；冒号后始终留空格。
  ```kotlin
  class FooImpl : Foo()
  abstract fun foo(a: Int): T
  val x = object : IFoo { ... }
  ```

  ### 4.4 类头
  - 参数少时单行；参数多时每个参数独立一行，缩进 4 格，右括号另起一行，继承/接口与右括号同行。
  - 超类型列表过长时，冒号后换行并横向对齐超类型名，类头与类体间可选加空行。

  ### 4.5 修饰符顺序
  ```
  public/protected/private/internal → expect/actual → final/open/abstract/sealed/const → external → override → lateinit → tailrec → vararg → suspend → inner → enum/annotation/fun → companion → inline/value → infix → operator → data
  ```
  注解放在修饰符前，且通常单独一行。

  ### 4.6 函数与属性格式化
  - 函数签名过长时，参数每行一个，返回类型前可换行。
  - 表达式函数体优先用 `=` 写法，过长则 `=` 留在第一行，体缩进 4 空格。
  - 属性 get/set 放不同行，初始化器过长则在 `=` 后换行缩进。

  ### 4.7 控制流
  - `if`/`when` 条件多行时，条件体使用花括号，条件右括号与左花括号放新行。
  - `when` 分支多行可用空行分隔，短分支可与条件同行且不加大括号。
  ```kotlin
  when (foo) {
      true -> bar()
      false -> { ... }  // 不推荐
  }
  ```

  ### 4.8 链式调用与方法调用
  - 长参数列表左括号后换行，参数缩进 4 空格。
  - 链式调用换行时，`.` 或 `?.` 放下一行并单倍缩进。

  ### 4.9 Lambda 表达式
  - 花括号与箭头两侧留空格，单个 lambda 参数尽量放括号外。
  - 多行 lambda 参数名放第一行，箭头后换行；参数列表太长则箭头单独一行。

  ### 4.10 尾部逗号
  - **【推荐】** 在声明中使用尾部逗号（参数、枚举、属性等），可使版本差异更清晰、便于添加/重排元素。

  ## 5. 文档注释 (KDoc)

  - **【强制】** 较长注释用 `/** ... */`，每行星号开头；短注释可单行。
  - **【推荐】** 避免 `@param` / `@return`，直接在注释中描述参数和返回值，使用 `[param]` 引用。

  ## 6. 语言特性惯用法

  ### 6.1 不可变性
  - **【强制】** 优先 `val` 声明，优先使用不可变集合（`List`、`Set`、`Map`），使用 `listOf()` 等不可变工厂函数。
  - **【强制】** 函数返回平台类型时，必须显式声明 Kotlin 类型。

  ### 6.2 默认参数
  - **【推荐】** 优先使用默认参数值而非函数重载。
  ```kotlin
  fun foo(a: String = "a") { ... }
  ```

  ### 6.3 字符串
  - **【推荐】** 优先模板 `$name ${expr}`；多行字符串优先使用 `trimIndent()` 或 `trimMargin()` 保持缩进。

  ### 6.4 条件与循环
  - **【推荐】** 利用 `if`/`when`/`try` 的表达式形式。
  - **【推荐】** 二元条件用 `if`，三个及以上选项用 `when`。
  - **【推荐】** `when` 守护条件中，组合多个布尔表达式时使用括号。
  - **【推荐】** 可空布尔判断用 `== true` 或 `== false`。
  - **【推荐】** 优先高阶函数 (`filter`、`map`) 而非循环；`forEach` 仅当接收者可为空或链式调用时使用。
  - **【推荐】** 循环区间使用 `..<` 操作符。

  ### 6.5 作用域函数
  - 根据场景选用 `let`, `run`, `with`, `apply`, `also`，对象配置或初始化用 `apply`，额外操作用 `also`，空安全调用链用 `let` 等。

  ### 6.6 其他惯用法
  - 嵌套 lambda 中显式声明参数，简短非嵌套 lambda 可用 `it`。
  - 避免在 lambda 最后一条语句使用返回到标签。
  - 函数多个相同原生类型或 Boolean 参数时，调用处使用具名实参。
  - 工厂函数命名应体现独特语义，不要与类同名（除非无特殊语义），优先用工厂函数替代多个重载构造函数。
  - 扩展函数广泛使用，但限制可见性。
  - 中缀函数仅用于角色相似对象（如 `to`, `and`）。

  ## 7. 库编写额外规范

  - **【强制】** 显式指定所有成员可见性及返回类型。
  - **【强制】** 公有成员提供 KDoc 注释，`override` 若无新文档可省略。

  ---

  *快速检查*：文件名匹配类名，4 空格缩进，花括号 K&R 风格，`val` 与不可变集合优先，使用 `?.` 和默认参数简化代码，公共 API 显式类型与注释。