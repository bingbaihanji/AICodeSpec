# MySQL 数据库设计与开发规范

## 1. 命名规范

### 1.1 通用规则
- **【强制】** 数据库名、表名、字段名统一使用小写字母、数字和下划线，禁止以数字开头，禁止两个下划线中间仅含数字。
- **【强制】** 表名不使用复数形式，应体现表内实体内容。
- **【强制】** 禁用 MySQL 保留字，如 `desc`、`range`、`match` 等。
- **【推荐】** 库名与应用名称尽量保持一致。
- **【推荐】** 表名遵循“业务名称_表的作用”，如 `alipay_task`、`trade_config`。
- **【强制】** 表达“是/否”概念的字段使用 `is_xxx` 命名，类型为 `unsigned tinyint`（1 是 0 否）。POJO 中对应布尔属性不加 `is` 前缀，需在 `resultMap` 中映射。
- **【参考】** 选择合适的存储长度，如：`tinyint unsigned`（0-255）、`smallint unsigned`（0-65535）、`int unsigned`（0-约43亿）、`bigint unsigned`（更大范围）。

### 1.2 索引命名
- **【强制】** 主键索引名：`pk_字段名`；唯一索引名：`uk_字段名`；普通索引名：`idx_字段名`。
- **【强制】** 联合索引名建议：`idx_字段1_字段2`，如 `idx_status_create_time`。

## 2. 建表规约

### 2.1 基础字段
- **【强制】** 每表必须包含三个字段：
  - `id`：主键，`bigint unsigned`，单表自增，步长为 1。
  - `create_time`：`datetime` 类型（如需时区信息用 `timestamp`）。
  - `update_time`：类型同 `create_time`，数据变更时须同步更新。
- **【强制】** 使用逻辑删除，增加 `is_deleted` 字段（`unsigned tinyint`），禁止物理删除。

### 2.2 数据类型
- **【强制】** 小数类型使用 `decimal`，禁止使用 `float`、`double`。
- **【强制】** 字符串长度几乎相等时使用 `char` 定长类型。
- **【强制】** `varchar` 长度不超过 5000，超出则用 `text` 并独立为一张表，通过主键关联，避免影响索引效率。
- **【强制】** 所有字段如果为非负数，须设为 `unsigned`。
- **【强制】** 字符集统一采用 `utf8mb4`。

### 2.3 其他约束
- **【强制】** 不得使用外键与级联更新，外键约束一律在应用层处理。
- **【推荐】** 修改字段含义或状态时，须及时更新字段注释。
- **【推荐】** 允许适当冗余字段以提升查询性能，但必须保证数据一致性。冗余字段应满足：非频繁修改、非唯一索引、非 `varchar` 超长字段（更不能是 `text`）。
- **【推荐】** 单表行数超过 500 万或容量超过 2GB 时才考虑分库分表，避免过早优化。

## 3. 索引规范

### 3.1 索引创建原则
- **【强制】** 业务上具有唯一特性的字段（含组合字段）必须建立唯一索引。
- **【推荐】** 经常作为查询条件、排序、关联的字段应建立索引。常见必要索引字段：`username`、`phone`、`email`、业务编码（如 `order_no`）、外键字段（如 `user_id`、`dept_id`）。
- **【强制】** `varchar` 字段建索引时必须指定索引长度，根据文本区分度计算（`count(distinct left(列名, 长度)) / count(*)`），一般长度 20 可达到 90% 以上区分度。
- **【强制】** 组合索引必须遵循最左前缀匹配原则。建索引时将等号条件字段前置、区分度高的字段放左边。
- **【推荐】** 利用覆盖索引避免回表，通过 `EXPLAIN` 观察 `Extra` 中的 `Using index`。
- **【强制】** 禁止在低区分度字段（如 `gender`、`sex`、`enable`、`deleted`）上单独建立索引。
- **【推荐】** 单表索引数量控制在 3~8 个，上限不超过 10 个。

### 3.2 索引使用与优化
- **【强制】** 页面搜索严禁左模糊或全模糊（`%xx`、`%xx%`），此类需求走搜索引擎。`LIKE 'xx%'` 可正常使用索引。
- **【强制】** 避免在索引列上使用函数或运算，导致索引失效（如 `WHERE YEAR(create_time) = 2025` 应改写为 `WHERE create_time >= '2025-01-01' AND create_time < '2026-01-01'`）。
- **【强制】** 防止字段类型隐式转换导致索引失效。
- **【推荐】** `ORDER BY` 字段应置于组合索引最后，利用索引有序性避免 `filesort`。
- **【推荐】** 超大分页（`OFFSET` 过大）采用延迟关联或子查询优化，先定位主键范围再关联原表。
- **【强制】** 上线前必须使用 `EXPLAIN` 检查执行计划，目标至少达到 `range` 级别，理想为 `ref` 或 `const`，避免全表扫描（`type=ALL`）。
- **【推荐】** 数据量达 100 万+ 时应关注索引优化，达 1000 万+ 需考虑分库分表、冷热数据分离、归档或 ES 检索等方案。

## 4. SQL 编写规范

- **【强制】** 不使用 `SELECT *`，必须显式列出所需字段。
- **【强制】** 统计行数使用 `COUNT(*)`，不用 `COUNT(列名)` 或 `COUNT(常量)`。
- **【强制】** 当某一列全为 NULL 时 `SUM(col)` 返回 NULL，需使用 `IFNULL(SUM(col), 0)` 避免 NPE。
- **【强制】** 判断 NULL 值使用 `ISNULL()`，不使用 `= NULL` 等直接比较。
- **【强制】** 分页查询当 `COUNT` 结果为 0 时应直接返回，不再执行后续分页语句。
- **【强制】** 数据订正（删除/修改）必须先 `SELECT` 确认数据无误，再执行更新操作。
- **【强制】** 多表查询时，须在列名前加表别名进行限定，推荐别名以 `as t1, t2, t3...` 顺序命名。
- **【强制】** 禁止使用存储过程。
- **【推荐】** `IN` 操作集合元素数控制在 1000 个以内。
- **【强制】** 超过三张表禁止 `JOIN`，关联字段类型必须绝对一致，且被关联字段必须有索引。
- **【参考】** 禁止在代码中使用 `TRUNCATE TABLE`，虽速度快但无事务且不触发触发器。
- **【参考】** 国际化场景使用 `utf8mb4`，计算字符数用 `CHARACTER_LENGTH()` 而非 `LENGTH()`。

## 5. ORM 映射规范（适用于 MyBatis/iBATIS）

- **【强制】** 查询时必须显式声明字段，不可使用 `*`；每个表必须定义对应的 `<resultMap>`，禁止直接使用 `resultClass`。
- **【强制】** POJO 布尔属性不加 `is`，数据库字段加 `is_`，须在 `resultMap` 中完成映射。
- **【强制】** SQL 参数占位符使用 `#{}`，严禁使用 `${}` 防止 SQL 注入。
- **【强制】** 更新表记录时，必须同时将 `update_time` 更新为当前时间。
- **【强制】** 不允许直接使用 `HashMap` 或 `Hashtable` 接收查询结果，应映射到明确实体。
- **【强制】** 不推荐使用 iBATIS 自带 `queryForList(String, int, int)` 分页实现（易 OOM），需在 SQL 层面实现分页。
- **【推荐】** 更新数据时不更新未修改的字段，避免错误、提高效率、减少 binlog 存储。
- **【参考】** 事务 `@Transactional` 勿滥用，使用时需全面考虑缓存回滚、搜索引擎回滚、消息补偿、统计修正等回滚方案。